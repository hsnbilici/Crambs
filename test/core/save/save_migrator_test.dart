import 'package:crumbs/core/save/save_migrator.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('SaveMigrator', () {
    test('scaffold placeholder — TODO per docs/save-format.md §6', () {
      expect(SaveMigrator, isA<Type>());
    });
  });
}
