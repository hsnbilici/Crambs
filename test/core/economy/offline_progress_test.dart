import 'package:crumbs/core/economy/offline_progress.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('OfflineProgress', () {
    test('scaffold placeholder — TODO per docs/economy.md §3.4', () {
      expect(OfflineProgress, isA<Type>());
    });
  });
}
