import 'package:flutter/material.dart';

/// Prestige ekranı — boş scaffold.
/// TODO: implement per docs/economy.md §9
class PrestigePage extends StatelessWidget {
  const PrestigePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Crumbs — Prestige'),
      ),
    );
  }
}
